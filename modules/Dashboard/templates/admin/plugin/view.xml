{* purpose of this template: plugins view xml view in admin area *}
{dashboardTemplateHeaders contentType='text/xml'}<?xml version="1.0" encoding="{charset}" ?>
<plugins>
{foreach item='item' from=$items}
    {include file='admin/plugin/include.xml'}
{foreachelse}
    <noPlugin />
{/foreach}
</plugins>
